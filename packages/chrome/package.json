{
  "name": "qrer-chrome",
  "version": "0.0.1",
  "description": "Chrome version of the QRer browser extension.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "cross-var bestzip dist/$npm_package_name-$npm_package_version.zip ./src/*"
  },
  "author": "Jason Wilson <jason@wilsons.io>",
  "license": "MIT",
  "devDependencies": {
    "bestzip": "^2.1.2",
    "cross-var": "^1.1.0"
  }
}
