{
  "name": "qrer-chrome",
  "version": "0.1.0",
  "description": "Chrome version of the QRer browser extension.",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "cd ./src && cross-var bestzip ../dist/$npm_package_name-$npm_package_version.zip ./*",
    "pack": "cd ./src && cross-var crx pack ./ -o ../dist/$npm_package_name-$npm_package_version.crx "
  },
  "author": "Jason Wilson <jason@wilsons.io>",
  "license": "MIT",
  "devDependencies": {
    "bestzip": "^2.1.7",
    "cross-var": "^1.1.0"
  }
}
